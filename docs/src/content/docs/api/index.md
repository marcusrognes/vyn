---
title: API reference
description: Every export, every option, every signature.
---

The API reference documents every public export of every Vyn package.
Each page is one package; sections within a page mirror the file structure
of the source.

Pages in this section will be added as packages stabilize:

- `vyn/core`
- `vyn/server`
- `vyn/client`
- `vyn/auth`
- `vyn/cli`
