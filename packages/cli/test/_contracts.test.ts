import { describe, it } from "vyn:test";

describe.todo(
	"vyn init — scaffolds files into the current directory, non-destructive",
);
describe.todo("vyn create <name> — mkdir + init combined");
describe.todo(
	"vyn dev — watch-mode server with HMR for routes, actions, components",
);
describe.todo("vyn build — produces a deployable artifact + hashed manifest");
describe.todo("vyn check — type-checks every file in the project");
describe.todo(
	"vyn gen — runs every codegen step (action barrel, route params, components)",
);
describe.todo(
	"vyn mcp --stdio — boots app, exposes MCP over stdio (no HTTP bind)",
);
describe.todo("Codegen — produces a single _vyn.gen.ts (not multiple files)");
